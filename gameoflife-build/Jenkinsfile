// Powered by Infostretch 

timestamps {

node () {

	stage ('SPAY-TEE/build - Checkout') {
 	 checkout([$class: 'GitSCM', branches: [[name: '*/master']], doGenerateSubmoduleConfigurations: false, extensions: [], submoduleCfg: [], userRemoteConfigs: [[credentialsId: '7bc9ea71-be0d-4653-be01-d76104012d0a', url: 'https://github.com/kiranp227/game-of-life.git']]]) 
	}
	stage ('SPAY-TEE/build - Build') {
 	
// Unable to convert a build step referring to "se.diabol.jenkins.pipeline.PipelineVersionContributor". Please verify and convert manually if required.		// Shell build step
sh """ 
sleep 5 
 """ 
	}
	stage ('SPAY-TEE/dockerize - Build') {
 	
// Unable to convert a build step referring to "se.diabol.jenkins.pipeline.PipelineVersionContributor". Please verify and convert manually if required.		// Shell build step
sh """ 
sleep 5 
 """ 
	}
	stage ('SPAY-TEE/promote-build3 - Build') {
 	
// Unable to convert a build step referring to "se.diabol.jenkins.pipeline.PipelineVersionContributor". Please verify and convert manually if required.		// Shell build step
sh """ 
sleep 5 
 """ 
	}
	stage ('SPAY-TEE/unit-tests-code-analysis - Build') {
 	
// Unable to convert a build step referring to "se.diabol.jenkins.pipeline.PipelineVersionContributor". Please verify and convert manually if required.		// Shell build step
sh """ 
sleep 5 
 """ 
	}
	stage ('SPAY-TEE/OWASP - Build') {
 	
// Unable to convert a build step referring to "se.diabol.jenkins.pipeline.PipelineVersionContributor". Please verify and convert manually if required.		// Shell build step
sh """ 
sleep 5 
 """ 
	}
	stage ('SPAY-TEE/sonarqube - Build') {
 	
// Unable to convert a build step referring to "se.diabol.jenkins.pipeline.PipelineVersionContributor". Please verify and convert manually if required.		// Shell build step
sh """ 
sleep 5 
 """ 
	}
	stage ('SPAY-TEE/send mail - Build') {
 	
// Unable to convert a build step referring to "se.diabol.jenkins.pipeline.PipelineVersionContributor". Please verify and convert manually if required.		// Shell build step
sh """ 
sleep 5 
 """ 
	}
	stage ('SPAY-TEE/dockerize-config - Build') {
 	
// Unable to convert a build step referring to "se.diabol.jenkins.pipeline.PipelineVersionContributor". Please verify and convert manually if required.		// Shell build step
sh """ 
sleep 5 
 """ 
	}
	stage ('SPAY-TEE/deploy - Build') {
 	
// Unable to convert a build step referring to "se.diabol.jenkins.pipeline.PipelineVersionContributor". Please verify and convert manually if required.		// Shell build step
sh """ 
sleep 5 
 """ 
	}
	stage ('SPAY-TEE/deploy-cassandra - Build') {
 	
// Unable to convert a build step referring to "se.diabol.jenkins.pipeline.PipelineVersionContributor". Please verify and convert manually if required.		// Shell build step
sh """ 
sleep 5 
 """ 
	}
	stage ('SPAY-TEE/bdd-tests - Build') {
 	
// Unable to convert a build step referring to "se.diabol.jenkins.pipeline.PipelineVersionContributor". Please verify and convert manually if required.		// Shell build step
sh """ 
sleep 5 
 """ 
	}
}
}