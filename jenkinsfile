pipeline{
    agent{label 'HRMS&&QA'}
    stages
    {
        stage('git')
        {
            steps{
                git branch: 'DeclarativePipeline', url: 'https://github.com/naresh86/game-of-life.git'

            }
        }
        stage('compile')
        {
            steps{
                sh mvn clean package
            }
        }

    }
}