pipeline{
    agent any
    stages{
        stage('Build')
        steps{
            sh "git --version"
            sh "mvn clean install -U"
        }
    }
post{
    always{
        CleanWs()
    }
}
}
